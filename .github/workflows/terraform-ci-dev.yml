name: Terraform CI Dev

on:
  push:
    branches:
      - main
    paths:
      - "terraform/**"

  pull_request:
    branches:
      - main
    paths:
      - "terraform/**"

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: eu-west-2
  AWS_ROLE_ARN: arn:aws:iam::557690622944:role/eks-v1-terraform-dev

jobs:
  terraform:
    name: Terraform Dev
    runs-on: [self-hosted, linux, x64]

    defaults:
      run:
        working-directory: terraform/envs/dev

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Diagnose GitHub OIDC claims
        working-directory: .
        run: |
          set -euo pipefail

          response=$(curl -sS \
            -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "$ACTIONS_ID_TOKEN_REQUEST_URL&audience=sts.amazonaws.com")

          token=$(printf '%s' "$response" | jq -r '.value')

          if [ -z "$token" ] || [ "$token" = "null" ]; then
            echo "ERROR: GitHub did not return an OIDC token."
            exit 1
          fi

          payload_b64=$(printf '%s' "$token" | cut -d '.' -f2 | tr '_-' '/+')
          while [ $(( ${#payload_b64} % 4 )) -ne 0 ]; do payload_b64="${payload_b64}="; done
          payload=$(printf '%s' "$payload_b64" | base64 -d 2>/dev/null)

          echo "GitHub OIDC claims:"
          printf '%s' "$payload" | jq '{iss, aud, sub, repository, repository_id, repository_owner, repository_owner_id, ref, event_name}'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Verify AWS identity
        run: aws sts get-caller-identity

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Format Check
        run: terraform fmt -check -recursive .

      - name: Terraform Init
        run: terraform init -input=false

      - name: Terraform Validate
        run: terraform validate

      - name: Checkov - Dev
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: terraform/envs/dev
          framework: terraform
          skip_check: CKV_AWS_39,CKV_AWS_130,CKV_AWS_51,CKV_AWS_339,CKV_AWS_136,CKV_AWS_37,CKV2_AWS_12,CKV2_AWS_11,CKV2_AWS_61,CKV_AWS_21,CKV_AWS_18,CKV2_AWS_62,CKV_AWS_144,CKV2_AWS_64,CKV_AWS_145
          soft_fail: false

      - name: Terraform Plan
        run: terraform plan -input=false -out=tfplan

      - name: Terraform Apply
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: terraform apply -input=false -auto-approve tfplan
